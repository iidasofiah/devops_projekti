spring:
  application:
    name: devopsdemo

  datasource:
    url: jdbc:postgresql://localhost:5432/devopsdb
    username: devops
    password: devops

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080